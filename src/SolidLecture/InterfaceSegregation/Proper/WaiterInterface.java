package SolidLecture.InterfaceSegregation.Proper;

// The interface in Violation subpackage can be broken into smaller
// interfaces such as these
public interface WaiterInterface {
    void takeOrder();
    void serveCustomer();
}
